<UserControl x:Class="RAP3.ResearcherDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RAP3"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel x:Name="StaffDetails">
        <StackPanel Orientation="Horizontal">
            <Label>Name</Label>
            <Label Content="{Binding Path=FirstName}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label>Unit</Label>
            <Label Content="{Binding Path=SchoolUnit}"/>
        </StackPanel>
        <ListView Grid.Column="0" x:Name="ResearcherPublications" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Year" DisplayMemberBinding="{Binding Doi}"/>
                    <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}"/>
                </GridView>
            </ListView.View>

        </ListView>
    </StackPanel>
</UserControl>
